/**
 * Provides protocol pack level support. Protocol packs can be loaded and
 * unloaded at runtime and this package provides support classes for managing a
 * group of defines protocol packs. The {@code PackId} class defines static
 * utility method for encoding and decoding pack and header information into a
 * globally unique numerical identifier.
 */
package com.slytechs.protocol.pack;