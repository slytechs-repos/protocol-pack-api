/**
 * Provides high level packet services. Packets are very common and low level
 * capture routines deliver packets in raw data form. This packet package,
 * provides high level packet services which allow the programmer to use a high
 * level API such as checking for presents of protocol headers and other high
 * level services (ie. IP fragment reassembly).
 */
package com.slytechs.jnet.protocol.packet;